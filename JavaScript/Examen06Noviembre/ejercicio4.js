/*Escribe una función que convierta un número en su representación binaria utilizando métodos del objeto Number.*/

function binario(){
    var num = 50;
    console.log(num.toString(2));
}
binario();